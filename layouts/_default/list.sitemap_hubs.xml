{{ printf "<?xml version=\"1.0\" encoding=\"utf-8\" standalone=\"yes\"?>" | safeHTML }}
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
{{- range .Site.RegularPages -}}
  {{- if eq .Section "posts" -}}
    {{- $name := "" -}}
    {{- with .File -}}{{- $name = .LogicalName -}}{{- end -}}
    {{- $isHub := or (or (hasPrefix $name "orphan-hub-") (hasPrefix $name "actress-hub-")) (hasPrefix $name "series-hub-") -}}
    {{- if not $isHub -}}
      {{- if findRE "actress-actress-|series-series-" $name -}}
        {{- $isHub = true -}}
      {{- end -}}
    {{- end -}}
    {{- if and (not (eq .Params.noindex true)) $isHub }}
  <url>
    <loc>{{ .Permalink }}</loc>{{ if not .Lastmod.IsZero }}
    <lastmod>{{ safeHTML (.Lastmod.Format "2006-01-02T15:04:05-07:00") }}</lastmod>{{ end }}{{ with .Sitemap.ChangeFreq }}
    <changefreq>{{ . }}</changefreq>{{ end }}{{ if ge .Sitemap.Priority 0.0 }}
    <priority>{{ .Sitemap.Priority }}</priority>{{ end }}
  </url>
    {{- end -}}
  {{- end -}}
{{- end }}
</urlset>
